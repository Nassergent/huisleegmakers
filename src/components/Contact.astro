---
// Contact sectie - schoon overstappen.nl stijl
---

<section id="contact" class="py-20 md:py-24 bg-white">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-14">
      <p class="text-sm font-semibold text-brand-600 uppercase tracking-widest mb-3">Contact</p>
      <h2 class="text-3xl md:text-4xl font-extrabold text-gray-900 tracking-tight font-display mb-4">Klaar voor een zorgeloze ontruiming?</h2>
      <p class="text-gray-500 max-w-xl mx-auto">Vraag vandaag nog een gratis offerte aan. Wij reageren binnen 24 uur.</p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-5 gap-8">
      <!-- Info -->
      <div class="lg:col-span-2 space-y-6">
        <a href="tel:+32478225633" class="flex items-center gap-4 p-4 bg-gray-50 rounded-xl border border-gray-100 hover:border-brand-200 hover:bg-brand-50/50 transition-all group">
          <div class="w-11 h-11 rounded-xl bg-brand-100 flex items-center justify-center group-hover:bg-brand-600 transition-colors">
            <svg class="w-5 h-5 text-brand-600 group-hover:text-white transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path></svg>
          </div>
          <div>
            <span class="block font-semibold text-gray-900 text-sm">0478 22 56 33</span>
            <span class="text-gray-400 text-xs">Spoed 24/7 bereikbaar</span>
          </div>
        </a>

        <a href="mailto:info@huisleegmakers.gent" class="flex items-center gap-4 p-4 bg-gray-50 rounded-xl border border-gray-100 hover:border-brand-200 hover:bg-brand-50/50 transition-all group">
          <div class="w-11 h-11 rounded-xl bg-brand-100 flex items-center justify-center group-hover:bg-brand-600 transition-colors">
            <svg class="w-5 h-5 text-brand-600 group-hover:text-white transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path></svg>
          </div>
          <div>
            <span class="block font-semibold text-gray-900 text-sm">info@huisleegmakers.gent</span>
            <span class="text-gray-400 text-xs">Reactie binnen 24 uur</span>
          </div>
        </a>

        <div class="flex items-center gap-4 p-4 bg-gray-50 rounded-xl border border-gray-100">
          <div class="w-11 h-11 rounded-xl bg-brand-100 flex items-center justify-center">
            <svg class="w-5 h-5 text-brand-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path></svg>
          </div>
          <div>
            <span class="block font-semibold text-gray-900 text-sm">Pinksterbloemstraat 3, 9030 Gent</span>
            <span class="text-gray-400 text-xs">Ma-Vr 08:00-18:00 &middot; Za 09:00-17:00</span>
          </div>
        </div>

        <div class="flex flex-wrap gap-4 pt-2 text-sm font-medium text-gray-500">
          <span class="flex items-center gap-1.5"><svg class="w-4 h-4 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg> Gratis en vrijblijvend</span>
          <span class="flex items-center gap-1.5"><svg class="w-4 h-4 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg> Geen verborgen kosten</span>
        </div>
      </div>

      <!-- Formulier -->
      <div class="lg:col-span-3">
        <form id="contact-form" class="bg-gray-50 rounded-xl border border-gray-100 p-6 md:p-8 space-y-4">
          <div class="hidden" aria-hidden="true"><input type="text" name="website" tabindex="-1" autocomplete="off" /></div>

          <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <div>
              <label for="naam" class="block text-sm font-medium text-gray-700 mb-1.5">Naam <span aria-hidden="true">*</span><span class="sr-only">(verplicht)</span></label>
              <input type="text" id="naam" name="naam" required aria-required="true" class="w-full bg-white border border-gray-200 rounded-lg px-4 py-3 text-sm focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all" placeholder="Uw naam" />
            </div>
            <div>
              <label for="telefoon" class="block text-sm font-medium text-gray-700 mb-1.5">Telefoon <span aria-hidden="true">*</span><span class="sr-only">(verplicht)</span></label>
              <input type="tel" id="telefoon" name="telefoon" required aria-required="true" class="w-full bg-white border border-gray-200 rounded-lg px-4 py-3 text-sm focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all" placeholder="Uw telefoonnummer" />
            </div>
          </div>

          <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <div>
              <label for="email" class="block text-sm font-medium text-gray-700 mb-1.5">E-mail</label>
              <input type="email" id="email" name="email" class="w-full bg-white border border-gray-200 rounded-lg px-4 py-3 text-sm focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all" placeholder="Uw e-mailadres" />
            </div>
            <div>
              <label for="locatie" class="block text-sm font-medium text-gray-700 mb-1.5">Locatie <span aria-hidden="true">*</span><span class="sr-only">(verplicht)</span></label>
              <input type="text" id="locatie" name="locatie" required aria-required="true" class="w-full bg-white border border-gray-200 rounded-lg px-4 py-3 text-sm focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all" placeholder="Stad of gemeente" />
            </div>
          </div>

          <div>
            <label for="type" class="block text-sm font-medium text-gray-700 mb-1.5">Type Ontruiming <span aria-hidden="true">*</span><span class="sr-only">(verplicht)</span></label>
            <select id="type" name="type" required aria-required="true" class="w-full bg-white border border-gray-200 rounded-lg px-4 py-3 text-sm focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all">
              <option value="">Selecteer type</option>
              <option value="woningontruiming">Woningontruiming</option>
              <option value="overlijden">Ontruiming na overlijden</option>
              <option value="bedrijfsontruiming">Bedrijfsontruiming</option>
              <option value="spoedontruiming">Spoedontruiming</option>
              <option value="anders">Anders</option>
            </select>
          </div>

          <div>
            <label for="beschrijving" class="block text-sm font-medium text-gray-700 mb-1.5">Beschrijving</label>
            <textarea id="beschrijving" name="beschrijving" rows="3" class="w-full bg-white border border-gray-200 rounded-lg px-4 py-3 text-sm focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all resize-none" placeholder="Beschrijf kort uw situatie"></textarea>
          </div>

          <label class="flex items-start gap-2.5 cursor-pointer">
            <input type="checkbox" name="privacy" required class="mt-0.5 rounded border-gray-300 text-brand-600 focus:ring-brand-500" />
            <span class="text-sm text-gray-500">Ik ga akkoord met het <a href="/privacy" class="text-brand-600 font-medium hover:underline">privacybeleid</a> *</span>
          </label>

          <button type="submit" id="submit-btn" class="w-full bg-brand-600 hover:bg-brand-700 text-white font-semibold text-base px-6 py-3.5 rounded-lg transition-all duration-200 shadow-sm hover:shadow-md">
            Plan Gratis Bezichtiging
          </button>
          <div id="form-message" class="hidden text-center text-sm font-medium py-3 rounded-lg" role="alert" aria-live="polite" aria-atomic="true"></div>
        </form>
      </div>
    </div>
  </div>
</section>

<script>
  const form = document.getElementById('contact-form') as HTMLFormElement;
  const btn = document.getElementById('submit-btn') as HTMLButtonElement;
  const msg = document.getElementById('form-message') as HTMLDivElement;

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    btn.disabled = true;
    btn.textContent = 'Verzenden...';
    msg.classList.add('hidden');

    try {
      const res = await fetch('/api/contact', {
        method: 'POST',
        body: new FormData(form),
      });
      const data = await res.json();

      if (res.ok) {
        msg.textContent = data.message || 'Uw aanvraag is succesvol verzonden!';
        msg.className = 'text-center text-sm font-medium py-3 rounded-lg bg-green-50 text-green-700 border border-green-100';
        form.reset();
      } else {
        msg.textContent = data.error || 'Er ging iets mis. Probeer het opnieuw.';
        msg.className = 'text-center text-sm font-medium py-3 rounded-lg bg-red-50 text-red-700 border border-red-100';
      }
    } catch {
      msg.textContent = 'Er ging iets mis. Bel ons op 0478 22 56 33.';
      msg.className = 'text-center text-sm font-medium py-3 rounded-lg bg-red-50 text-red-700 border border-red-100';
    }

    btn.disabled = false;
    btn.textContent = 'Plan Gratis Bezichtiging';
  });
</script>
